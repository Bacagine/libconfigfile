# libconfigfile

libconfigfile is a C library created to read a .conf files

## Building and install

`$ make`

`# make install`

## Test
`$ make test`

`$ make run`

## Uninstall
`# make uninstall`

